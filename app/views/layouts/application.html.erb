<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Recipe Scribe</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="top-bar">
      <div class="top-bar-left">
        <ul class="dropdown menu" data-dropdown-menu>
          <li class="menu-text"><h4><%= link_to "Recipe Scribe", root_path %></h4></li>
          <li><%= link_to "Add Recipe", new_recipe_path, id: "add_recipe" %></li>
          <li><a href="#">Two</a></li>
          <li><a href="#">Three</a></li>
        </ul>
      </div>
      <div class="top-bar-right">
        <ul class="menu">
          <li><input type="search" placeholder="Search"></li>
          <li><button type="button" class="button">Search</button></li>
        </ul>
      </div>
    </div>
      <% if flash[:notice] %>
        <div class="flash_notice success callout" data-closable>
          <% flash.each do |_key, value| %>
            <%= value %>
          <% end %>
        </div>
      <% end %>
      <% if flash[:error] %>
        <div class="flash_notice alert callout" data-closable>
          <% flash.each do |_key, value| %>
            <%= value %>
            <button class="close-button" aria-label="Dismiss alert" type="button" data-close>
              <span aria-hidden="true">&times;</span>
            </button>
          <% end %>
        </div>
      <% end %>
      <%= yield %>
  </body>
</html>
